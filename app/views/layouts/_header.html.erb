
<% if logged_in_user? %>
	<%= link_to "MEALS", meals_path %>
	<%= link_to "GOAL", current_user.goals.first.nil? ? new_goal_path : goal_path(current_user.goals.find_by(current_goal: true)) %>
	<%= link_to "LOGOUT", destroy_user_session_path, method: :delete %>
	<%= link_to "HELP", help_path %>
<% end %>

